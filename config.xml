<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="https://sade.textgrid.de/ns/configfile">
  <!-- global parameters -->
    <!-- unless you know exaclty what you are doing, leave the project-id as
         it is. -->
    <param key="project-id">textgrid</param>
    <param key="project-title">SADE Referenzinstanz</param>

    <param key="textgrid.tgcrud">https://textgridlab.org/1.0/tgcrud/rest</param>
    <param key="textgrid.webauth">https://textgridlab.org/1.0/WebAuthN/TextGrid-WebAuth.php</param>
    <param key="textgrid.authZinstance">textgrid-esx2.gwdg.de</param>
    <param key="textgrid.public-triplestore">https://textgridlab.org/1.0/triplestore/textgrid-public</param>
    <param key="textgrid.nonpublic-triplestore">https://textgridlab.org/1.0/triplestore/textgrid-nonpublic</param>
    <param key="textgrid.search-nonpublic">https://textgridlab.org/1.0/tgsearch/</param>
    <param key="textgrid.search-public">https://textgridlab.org/1.0/tgsearch-public/</param>

    <!--  if you want to distinguish between develop and productive instance,
          set sade.develop "true"
          use it for example to set up a protected instance -->
    <param key="sade.develop">true</param>
    <!--  if sade.develop is set to true and you want to look behind the curtain
          you probably want to set a password like secret to authenticate -->
    <param key="secret">Mellon</param>

    <param key="textgrid.digilib">https://textgridlab.org/1.0/digilib/rest/IIIF/</param>
    <param key="textgrid.digilib.defaultSize">1500,</param>
    <param key="textgrid.digilib.tile" deprecated="true">dw=100&amp;dh=100</param>

    <!-- set if you want to preserve revisions in the database.
         if set to "false" all resource names will be trimmed to base-uris and
         the database will always store the last published item per base uri -->
    <param key="textgrid.preserveRevisions">false</param>

    <module key="multilanguage">
        <param key="lang.enabled">false</param>
        <description>
            if "lang.enabled" set to true (default=false), all requests and links in the response will be redirected
            to the choosen or default language. in the standard template, a
            switch will be present in the top menu.
        </description>
        <param key="lang.default">de</param>
        <param key="lang.alt" description="a semicolon seperated list of alternative languages">en</param>
    </module>

  <!-- parameters for MODULES -->
    <module key="navigation">
        <!-- Where to find the xml with the navigation template -->
        <param key="location">navigation.xml</param>
    </module>

    <module key="wiki">
        <!-- sample configuration for Confluence -->
        <param key="confluence.url">https://wiki.de.dariah.eu/rest/api/content/</param>
        <param key="confluence.lang" description="if the multilanguage plugin is enabled, used languages specified here">
            <lang code="de" name="german"/>
            <lang code="en" name="english"/>
            <lang code="fr" name="french"/>
        </param>

        <!-- sample configuration for DokuWiki -->
        <param key="dokuwiki.url">https://www.dokuwiki.org</param>
        <param key="dokuwiki.space"></param>
        <param key="dokuwiki.whitelist">
          <item>start</item>
          <item>features</item>
        </param>
    </module>

    <module key="multiviewer">
        <param key="xslt">
            <stylesheet namespace="http://www.tei-c.org/ns/1.0" location="_assets/TEI-stylsheets/html5/html5.xsl"/>
        </param>
    </module>

    <module key="faceted-search">
        <!-- how much kwic hits to show per document -->
        <param key="hits-per-page">10</param>
        <param key="kwic-hits">3</param>
        <param key="kwic-width">20</param>
        <param key="query-root">
          <xpath>//tei:TEI</xpath>
        </param>
        <param key="result-title">
            <xpath>//tei:fileDesc//tei:titleStmt/tei:title/text()</xpath>
        </param>
        <param key="result-xslt">search-hits.xslt</param>
        <param key="thumbnail">false</param>
        <param key="viewer-html">content.html?id=/data/</param>

        <param key="facets">
            <facet key="authors" title="Authors">
                <xpath>tei:author</xpath>
            </facet>
            <facet key="keywords" title="Schlagwort">
                <xpath>tei:term</xpath>
            </facet>
            <facet key="dates" title="Zeit">
                <xpath>tei:date/@when</xpath>
            </facet>
            <facet key="places" title="Publikationsort">
                <xpath>tei:pubPlace</xpath>
            </facet>
        </param>
    </module>

    <module key="callgraph">
        <!-- the dot command on command line (defaults to Unix) -->
        <param key="dot-cmd">dot</param>
        <param key="target-dir"/>
    </module>
</config>
